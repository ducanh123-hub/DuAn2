<?xml version="1.0" encoding="UTF-8"?>
<web-app xmlns="https://jakarta.ee/xml/ns/jakartaee"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="https://jakarta.ee/xml/ns/jakartaee
             https://jakarta.ee/xml/ns/jakartaee/web-app_5_0.xsd"
         version="5.0">

    <!-- ============================================================
         FILTER DECLARATIONS
         ============================================================ -->

    <filter>
        <filter-name>AuthFilter</filter-name>
        <filter-class>com.hotel.filter.AuthFilter</filter-class>
    </filter>

    <filter>
        <filter-name>AdminFilter</filter-name>
        <filter-class>com.hotel.filter.AdminFilter</filter-class>
    </filter>

    <filter>
        <filter-name>CustomerFilter</filter-name>
        <filter-class>com.hotel.filter.CustomerFilter</filter-class>
    </filter>

    <!-- ============================================================
         FILTER MAPPINGS — thứ tự ở đây quyết định chuỗi filter:
         1. AuthFilter    → kiểm tra đã đăng nhập chưa
         2. AdminFilter   → kiểm tra quyền Admin
         3. CustomerFilter → kiểm tra quyền Customer
         ============================================================ -->

    <filter-mapping>
        <filter-name>AuthFilter</filter-name>
        <url-pattern>/*</url-pattern>
    </filter-mapping>

    <filter-mapping>
        <filter-name>AdminFilter</filter-name>
        <url-pattern>/*</url-pattern>
    </filter-mapping>

    <filter-mapping>
        <filter-name>CustomerFilter</filter-name>
        <url-pattern>/*</url-pattern>
    </filter-mapping>

</web-app>